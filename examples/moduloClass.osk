load("examples/gcd.osk")
load("examples/totient.osk")

n = num(input("Enter n: "))

list = []

j = 0
for i=0,n do
  if gcd(i,n) == 1 do
    set(list, j, i)
    j += 1
  end
end

print("phi(n) =", phi(n))
print(list)